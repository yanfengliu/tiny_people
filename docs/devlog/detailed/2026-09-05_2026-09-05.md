# Devlog — 2026-09-05

## Model-only presentation and actual camera translation

**Timestamp:** 2026-09-05 14:52–15:13 America/Los_Angeles

**Action:** Applied the user's model-only revision: removed the masthead, headline, toolbars, statuses and visible keyboard help, plus mesh lettering on buttons, rail and PCB and the tiny house-number marks. Nonvisual accessibility metadata remains, with visible diagnostics only on graphics failure. WASD now works immediately and translates camera/target together in the horizontal controller plane. Reset centers and fits the model bounds; arrows, zoom, Space pause, reduced motion and graphics/history recovery remain available.

**Result:** Runtime source passed independent source review. Seventeen browser captures pass camera/motion checks and show visible production panning while resident life is paused; releasing D leaves identical rendered pixels. The current model renders in 476 calls and 451,186 triangles. A bounded 150-frame desktop sample averaged 16.665ms with p95 17ms. Build/typecheck and unchanged route/resident contact gates passed; the 611KB minified entry retains Vite's unsuppressed size advisory.

**Validation:** The desktop exploration gate replaces obsolete visible-button assertions with thirteen required groups and six captures. Eighteen measured translations cover immediate WASD without focus, directions after orbit, equal camera/target offsets, opposite-key cancellation, normalized diagonals, repeat events, partial/uppercase release, editable/modifier exclusions and held-key clearing after blur, visibility, persisted pagehide and context loss. Native callback strides produce roughly 16.7/33.3/66.7ms frame partitions: movement remains .22 times viewing distance per integrated second, with the intended 50ms per-frame cap. Resident pause remains distinct from test-clock freeze. Actual history back and synthetic persisted events are reported separately.

**Code reviewer comments:** Independent review found otherwise easy-to-miss house-number tally marks. Native inspection found the first portrait composition too small, which was corrected before the user removed mobile support from scope. The manager accepted the model-only desktop presentation and panning implementation; no further presentation polishing or realism changes belong to this phase.

**Scope update:** The user's later instruction, “No need to support mobile version,” makes desktop the acceptance target. Dedicated mobile/touch gate branches and new mobile captures were removed explicitly. Existing harmless responsive defaults and prior mobile evidence are retained. The manager's helper owns local Git integration; this worktree made no index, branch, commit or remote changes. People and plant realism remains phase six.

## Exploration must survive real input and graphics recovery

**Timestamp:** 2026-09-05 12:01–12:27 America/Los_Angeles

**Action:** Added a deterministic source-generated fine plastic grain, neutral HDR reference lighting, distinct charcoal/rubber surfaces, salmon rail/collar and richer green/gold circuitry. A restrained rubber-only bounce term keeps recessed controls readable without changing global exposure. Added overview/café/courtyard/circuit-home views, zoom controls, intro recession, backed overlays, visible keyboard help on scene focus, pause/resume and live reduced-motion behavior. All eight controls fit short phone landscape without shrinking below 44px.

**Result:** The final 14-view browser gate passed independent camera transforms, exact frozen reset, nine moving walkers, disabled-input controls, mobile composition and zero console/network errors. Rendering remains 528 calls and 455,962 triangles; the local 150-frame sample averaged 16.672ms with p95 17.1ms. Build/typecheck and dependency audit passed; the 612KB minified entry still triggers Vite's unchanged 500KB advisory. No bundle-size warning was hidden, and these local timings are not a hardware-wide guarantee.

**Validation:** The new exploration gate checks eleven behavior groups with thirteen captures: ordinary preset actions, real UI/keyboard pause distinct from test freeze, focus traversal, actual CDP touch orbit/pinch, 390×844/844×390/667×375 layouts, fresh reduced-motion and live media changes, real history return, persisted-page suspension, unavailable WebGL, and real context loss/restoration followed by cleanup. Actual history returned through reload (`persisted: false`); separately dispatched persisted events preserved the same canvas and resumed time. Source route and resident contact gates remained green.

**Code reviewer comments:** Reference review requested local lifts to black rubber shadows and a shift from orange-red toward salmon while keeping green, clothing and neutral surroundings stable. The expanded gate then exposed 257 stale GPU-handle deletion warnings after successful restoration. A focused two-cycle probe isolated the failure to later disposal. Releasing GPU allocations and listeners during context loss, while preserving CPU scene data for restoration, removed all warnings. Independent visual review also prompted accurate graphics-failure status and sighted keyboard guidance.

**Notes:** Every browser was headless and identified by its own PID; contexts, browsers and in-process Vite servers closed in `finally`. Geometry-only Vite loaders now disable unnecessary HMR to avoid competing for a shared port. Ignored manifests bind captures to review evidence; rejected context-disposal evidence is retained compactly. The manager owns phase acceptance and integration into the user's checkout. No commit, push or publication was performed.

## A community needs contact, not only visible people

**Timestamp:** 2026-09-05 11:35–11:57 America/Los_Angeles

**Action:** Added 26 residents (nine walkers and 17 local activities), a café, joystick courtyard, two circuit-board homes, laundry, seating, plants and a ramp between decks. Community furnishings are merged by material and residents use four shared instanced batches.

**Result:** Rendering rose from 502 calls to 528 including shadows. Seven routes passed 1,089 spatial samples with nine physical footprint rays each, and a 240-second actor-pair check kept minimum separation above .21 units. The initial phase-two local 150-frame check averaged 16.67ms with p95 17.4ms. These figures describe the recorded desktop Chromium run, not a universal performance guarantee.

**Validation:** Separate geometry gates inspect full authored routes against real meshes, stationary pelvis/torso volumes, transformed shoe soles on signed slopes, and seated thighs/shins. Positive controls put a route inside the joystick and off the device. Mutation controls move the reading book into the pelvis and restore the old low knee. Frozen world time and independent camera transforms make camera checks insensitive to resident motion; disabled controls are proved unchanged while residents continue to walk.

**Code reviewer comments:** The manager caught a book intersecting a seated pelvis, thighs below seat tops and flat-ground shoe poses on the ramp. Native close views also exposed coplanar landing artifacts, and a separate reviewer identified empty watering hands. Repairs repositioned both bench books, raised knees, aligned ankles/soles to the actual slope, offset supported landing decks .014 units and added small watering cans. A broad bounding-box check initially filled the tray cavity and falsely reported every interior resident colliding; actual triangle/solid-volume checks now preserve that hollow space.

**Notes:** The user replaced the reference with an HDR-adjusted `nintendo.png`, 2418×1354, SHA-256 `86CE70112D28809CE994AA3821B06AA6FF759FD20AF84BCE815943D199AAF14F`. It was re-inspected and resident clothing changed to vivid cyan/yellow/orange. Full charcoal/coral/green material and lighting calibration remains the next phase, along with the already-known exploration UI and accessibility refinements.

## The exposed board required a genuinely hollow shell

**Timestamp:** 2026-09-05 11:20–11:34 America/Los_Angeles

**Action:** Built the reference-led device in Three.js from reusable geometry, vector lettering and materials. Added a Vite/TypeScript application, responsive presentation, source-only ignore rules, lockfile and a headless browser check.

**Result:** The initial solid lower shell covered the PCB despite correct board coordinates. Replaced that solid with a thin floor, hollow walls and seam, plus board supports. Narrow-screen framing now accounts for aspect ratio and shows the complete device at 390×844. Reset clears pending orbit damping so it reproduces the initial rendered pixels exactly.

**Validation:** Build and strict typecheck passed. Dependency audit found zero vulnerabilities. The browser check exercises actual drag, zoom and reset, captures seven bound views, verifies mobile canvas/no horizontal overflow and rejects console/network errors. Native-resolution visual inspection confirms PCB visibility and device framing; screenshot hashes bind that review to the captured bytes. This phase does not establish future population animation, touch flows, reduced-motion behavior or final material polish.

**Code reviewer comments:** The manager independently caught the buried PCB and mobile cropping during the first render. A separate read-only reviewer approved the corrected controller and cavity, correct control layout and complete desktop/mobile framing. They identified title overlap during close exploration, flat material/contact-shadow treatment and desktop wording on mobile as later exploration/polish work.

**Notes:** The Playwright CLI printed help but crashed during Windows process shutdown, including when invoked directly. The reusable check therefore uses the pinned Playwright library, an identified headless browser process and an in-process Vite server, all closed in `finally`. Shared browser processes are never stopped. This work changes neither the manager's canonical plan nor the original reference image and does not commit, push or publish.
