# Devlog — 2026-09-08

## Equal pictures concealed repeated social replay

**Timestamp:** 2026-09-08, America/Los_Angeles.

**Action:** Prepared the phase-10 candidate for original-checkout integration after independent source review 33 reproduced repeated social-history rebuilding during paused rendering. B compares requested time at the model's nanosecond precision and separates presentation from explicit seek and restore. C retains those exact wrapper and time-gate inputs.

**Result:** Three updates at raw time `10.1234567896` previously caused three resets and 909 fixed ticks despite equal snapshots and poses. The focused B review observes zero resets/ticks and three presentations. Explicit seek and restore each replay once. Reinstating the raw comparison reproduces the original count failure. The manager accepts F31 within this scope; a separately retained C-bound producer time report passes the same nine cases and corruption control.

**Reasoning:** Pixel and state equality establish unchanged results, not the absence of repeated work. The regression check reaches actual model reset/step entries and proves its instrumentation ran. Raw invalid input must still reach validation because a small negative time can round to negative zero.

**Validation:** Review 36 independently checks B and its original counterexample. Review 37 verifies unchanged repair inputs plus C time report `1b234ba6793749ef7bc3bd403a3f44fada32214daade26e306e3798c03c71f67`. This is bounded CPU evidence; final root native input, lifecycle and measured frame performance remain pending.

**Code reviewer comments:** The original model equality checks and browser pause comparisons could pass after an unnecessary rebuild. A canonical comparison and a presentation path that cannot replay authority close that specific gap without claiming zero rendering work.

## Valid gardening contact did not establish useful cooperation

**Timestamp:** 2026-09-08, America/Los_Angeles.

**Action:** C moves the existing helper stage before preparation and watering. No stage duration, pose, prop, target, approach or supported circuit position changes. The manager closes F32 through source review 37's narrow repair/old-model controls and independent native review 38.

**Result:** Both real garden pairs complete guidance, preparation, water, drain, lowering and acknowledgment. The 240-second state and contact runs each observe 16 completed garden cycles. Order is checked for all completed cycles; detailed emitted gardening geometry samples the first completed cycle of each pair. Both old-order controls reproduce exact B model bytes and reject its sequence even though its geometry still passes.

**Reasoning:** The helper's point needs to precede the gardener's response. Stage names and valid hand/can contact cannot establish that temporal reading. The circuit residents keep their supported stationary homes because the earlier moving approach was rejected at raised traces.

**Validation:** Reviews 35 and 37 retain the original finding and source repair separately. The producer's C native report observes ordinary autoplay and inspects 12 original-resolution stage images. Independent review 38 inspects all 20 native PNGs plus 12 discrete decoded frames, and the manager separately inspects help, water and acknowledgment for both pairs. These sampled views support quiet but readable guidance before tending and a supported dry finish. Neither video was watched continuously. Root controls and measured performance remain separate acceptance conditions.

**Code reviewer comments:** The earlier choreography looked like watering followed by a gesture. Source repair and CPU sequence checks supplement, rather than replace, the fresh native visual judgment.

## Fixed timing assumptions failed on a faster native frame clock

**Timestamp:** 2026-09-08, America/Los_Angeles.

**Action:** Run the normal root browser, exploration and mechanism-input gates sequentially on exact C source and its production build. Preserve both failed outputs before any harness repair or rerun.

**Result:** The browser gate passes. Exploration's fixed stride-four clamp trial records 97 frames averaging 16.675258 ms and zero capped intervals, so its required eight intervals above 50 ms never occur. Mechanism-input's fixed 450 ms command cadence fits only six commands into 600 native frames; twelve are required. Its baseline mean is 4.166667 ms, active mean is 4.8125 ms, and 598 frames contain motion. Eighteen preceding input/lifecycle groups and 100 resource cycles finish without errors or resource growth. The full gates remain failed and their later groups incomplete.

**Reasoning:** A fixed frame count does not imply a fixed elapsed duration. Both assertions correctly reject missing stimulus at the observed faster native cadence. Neither failure establishes a runtime clamp or measured performance-budget defect. The user then requires dynamic support for low, high and changing native cadence, so changing monitor refresh alone cannot fail a test. This also reaches v2's absolute timing thresholds and sequential paired-window assumptions. Preserve the old thresholds and results as history while reviewing a monitor-independent contract that retains real input, raw timestamps, required coverage and meaningful workload-regression checks. The durable direction is recorded in `docs/policies/local-rules.md`.

**Validation:** Failed exploration evidence `e8673d17fd780cecfa0f67de0ca5fa0b848a74efb3daa268627d6fc02d1f9cec` and mechanism-input evidence `cf669475d13f2221694c9fee453101ffe09951a0dca9aa04262dbf8e7a41bb88` remain in separate `output/manager/phase10-integration/` failure archives. Their source/build maps remain exact C and cleanup records zero owned leftovers. These original failures remain separate from the later R2 passes below.

## Completed CPU work replaces monitor waiting in the budget

**Action:** Integrate reviewer-admitted R2's twelve-path delta after reconstructing all 48 final files from exact C. The new development-only recorder measures synchronous updates through return from render submission. Native periods and the unfiltered paired CPU mean difference are diagnostic only. The earlier proposed +1 ms CPU delta gate was superseded before freeze because cadence changes fixed-tick work per callback; there is no matched-workload overhead or GPU-completion claim.

**Result:** Typecheck, build, the CPU budget evaluator, browser, exploration and normal mechanism-input all pass. Production HTML, JavaScript and CSS remain byte-identical to C. Thirty-three unchanged dependency inputs bind twelve earlier physical/social/audit passes. Browser records 17 views; exploration completes 13 groups and 18 translations. Mechanism-input completes 20 groups and 33 captures with no errors or violations. Baseline150/active600 CPU means are 1.715333/2.208500 ms, p95 2/2.5 ms and maxima 2.2/2.9 ms; no sample exceeds 20 ms. Fourteen trusted commands land inside the active window with six-bin counts 3/2/2/2/3/2 and 592 moving frames. One hundred real rail cycles preserve 270 geometries, 18 textures and 14 programs. Native means 4.166667/4.166833 ms and paired CPU difference +0.493167 ms are diagnostics only.

**Code reviewer comments:** The first immutable repair accidentally shared the progress emitter's module instance with a CPU preflight, consuming its first four sequence values. The unchanged root parser correctly rejects first-row 5. R2 isolates the proof module and reproduces old rejection/new first-row 1 acceptance. The original rejected package remains unchanged; it was never integrated. Independent source admission also verifies rejection of reused or overlapping paired work windows.

**Validation:** The six root wrappers bind all 48 source files before/after, capture actual zero exits and record zero owned leftovers. Mandatory in-gate CPU proofs cover low/high/changing cadence, observation mutations, the actual runtime clamp and malformed work records. Relevant-action progress replaces the fixed whole-suite deadline; callback stalls remain incomplete verification. Final mechanism evidence is `956251f9c007035cda012a6954c00c232c15101e2bcee3c9699af639240436a0`. Actual history return reloads (`sameDocument=false`, `persisted=false`); synthetic persisted events are separate. Final integrated review 41 and manager acceptance remain pending.

## Verification loaders share a mutable cache

**Timestamp:** 2026-09-08, America/Los_Angeles.

**Action:** Preserve the producer's concurrent route-gate `EPERM rename` failure and its clean sequential C rerun. Prepare original-checkout Vite-based gates to run sequentially and retain old default-path evidence before replacement.

**Result:** The clean route report binds C's eight recorded inputs and passes the seven legacy routes, actual poses, separation and movement checks over 240 seconds. The earlier cache failure was an execution race, not a route defect. No global configuration or cache-cleanup workaround was needed.

**Notes:** C contains 41 frozen source files and unchanged dependencies. The delivery owner independently reconstructs all 41 from 32 actual-base files and the 23-path patch, copies 20 changed root paths, and leaves three already-exact raw files untouched. Forty prior output/build files totaling 9,296,392 bytes are preserved before gate output replacement. Combined acceptance remains pending. Local integration is separate from the rejected twelve-commit publication payload through `9d661d9`; later local work must not silently expand that payload.
