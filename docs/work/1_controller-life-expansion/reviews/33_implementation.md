# Review 33: implementation

## Target

Frozen phase-10 checkpoint A2 in `C:/Users/38909/.codex/worktrees/dead/tiny_people/output/phase10/checkpoint-a2/source/`, against accepted runtime base `2e57ccd555a9cc4300abc828916631fb8f2eb097`. The primary repository is `C:/Users/38909/Documents/github/tiny_people`. A2 contains 40 files and 505,596 bytes; its adjacent `source-manifest.json` SHA-256 is `e3eff21f78f60843b2faa5d9c4c23fae08b0593f95a7a6bac56bc956297e74c5`. The retained `changes-from-2e57ccd.patch` SHA-256 is `be515b8549aaaf5d65afb4c776020ea0327e37e448a40a9861b371636863562f`, covering eight added and fourteen modified files.

The assigned source scope is authoritative social state and types, authored layout and community integration, the pose adapter and resident renderer boundary, actual `main.ts` event/history wiring, and the meaning of the social and adjacent lifecycle checks. Source hashes central to the finding are `src/scene/community.ts` = `a70379e615f1d8be9d8c985fca4dbe19f45e44f0514456419eae668eab73ec6b` and `src/scene/social-state.ts` = `9313c85d7e58b1ea022d19b090bb9c5fe4605ca07d9d78bd8adc70b23daa45c1`. The reviewed main-loop hash is `0394c96ec006f0d6ba8f7e1fdba863dec92f2a41613c718c674597d62a354678`; pose-adapter hash is `b5d81aa4c327fce96e136496dcbf73a2947c06434d17336ca1974444af756dc8`; resident-renderer hash is `1f537e3c97da72c2c927caa38e382fe888bf937a537365ef4c5aff5d11f750f4`.

All four bridge inputs match the hashes accepted in [round 32](32_implementation.md). That unchanged bridge review is retained and not repeated here. A subsequently proposed F31 repair is outside this frozen target and requires its own exact handoff and focused re-review.

## Reviewers and coverage

`/root/phase10_source_recovery` completed this independent source review after the earlier assigned reviewer stopped. It read the primary AGENTS/local rules, the canonical phase plan and Fleet review format, verified the frozen source inventory, checked all fourteen changed existing inputs against their actual base blobs, inspected the model/layout/adapter/renderer and main call sites, examined the producer's retained social reports and test bodies, and ran one bounded CPU-only exact-source probe. The probe uses Node 24.12.0 TypeScript stripping and extension resolution. Its only production-code instrumentation increments counters at model reset and fixed-step entry; it does not replace model decisions, scene geometry or community update logic.

The former `/root/exploration_review` established F31 and left its probe and result. It did not produce a complete review-33 report before stopping. Its finding is separately attributed below; this report does not invent or extend an unwritten earlier review. The recovery reviewer independently reproduces that finding and owns the other coverage stated here.

No product source, plan, AGENTS file, Git index or history was changed. No browser, visible application, Vite instance, network listener, server or GPU workload was launched. The full unmodified 240-second suites were not rerun. Their recorded results are inspected producer evidence. Native visual quality, real browser controls, graphics/BFCache behavior and performance remain outside this CPU source acceptance; assigning a camera or direct state cannot establish the control paths those actions bypass.

## Reports

### Earlier partial finding from `/root/exploration_review`

The retained `output/manager/phase10-checkpoint-a2-review/paused-replay-report.json`, SHA-256 `e012cb2b1986fe611505f986a6c00d784cc2584e2c4c34849322c2fd9902d00d`, records F31 on the actual frozen community/controller/model. Its `paused-replay-probe.mjs` is `a93d1fa837755047ae1e0457ebd48c7820753a8d94908dfc6a99506f32b2d60c`. At raw time `10.1234567896`, the model stores `10.12345679`; three equal-time community updates cause three resets and 909 fixed ticks. At raw time `11.1234567894`, the stored value is `11.123456789`; three equal-time updates cause zero resets and zero ticks. Both cases preserve the visible social frame. These exact prior files remain unchanged.

### `/root/phase10_source_recovery`

F31 remains a material defect in A2. `community.ts:338` compares raw caller time with the model's rounded frame time before selecting seek or advance. A time that rounded upward therefore appears to move backward on every paused render. `social-state.ts:231–239` already canonicalizes advancement consistently and performs an equal-time no-op; the wrapper bypasses that behavior by choosing `seek`. Each repeated seek resets and rebuilds the model from zero to the paused time. The same wrapper is also called immediately after `community.seek`, so a rounded-up explicit seek does a second replay. The cost grows with elapsed simulated time. The finding is work repeated by the CPU; it is not evidence that the paused residents visibly change or that a particular browser frame budget was measured and exceeded.

The independent probe reproduces the original two cases exactly. It also confirms that pure-model equal-time calls at `75.1234567896` and its canonical `75.12345679` perform zero resets and zero ticks. This separates the wrapper defect from the model clock contract. Existing social-state equality checks and the browser pause assertions compare state or pixels, both of which remain equal after an unnecessary replay. The repair therefore needs a check on actual community seek/reset/fixed-step work for times on both sides of the rounding boundary, including explicit seek followed by repeated render updates. A canonical time interface and a presentation path that cannot replay authority are reasonable narrow repairs, subject to exact-source review.

The model has a coherent authority boundary within the inspected source. It copies its input layout, requires stable IDs 0–25 exactly once, sorts residents by ID, generates deterministic traits from ID, and advances decisions on 30 Hz ticks. The scene publishes a recursively frozen layout and copied footprints. Six opportunities use atomically reserved place, participant, seat and applicable prop resources. An actor points back to the interaction that reserves it; completion releases those claims and restores its base activity. Arbitration includes elapsed waiting time and a deterministic final order. Choices, trait preferences, reactions, stages, prop owner and droplet ages are selected upstream of the pose adapter.

Five explicit home-to-contact approaches preserve the seven independent legacy routes. Approaching actors turn before translation, advance along supported polylines, orient toward their partners, and leave along the same path before restoring home orientation and releasing reservations. A blocked approach has a deadline and enters retreat. The finite deadline alone does not prove every conceivable exit can complete; the retained obstruction control proves one actual café blockage retreats and releases, and the ordinary 240-second run proves the authored opportunities complete within that bound. The stationary circuit pairs retain their supported homes, preserving the recorded rejection of an unsupported moving approach around raised traces.

All four required behavior families have model paths. Greeting includes lowering and restoring an existing book where applicable, a greeting, and reciprocal speaking/listening stages. Café service moves one shared logical cup through offer, receive, drink, return and acknowledgment; owner changes occur at the receive/return boundaries. Gardening has finite preparation, water, drain, lowering, acknowledgment and helper stages, with at most three model-aged drops and a soil target for helping. Opening reactions use curiosity for notice, delay and duration, defer while an interaction is active, preserve up to one pending reaction per mechanism and resume base activity after completion. The raw opening journal is retained separately from that bounded pending response set. These stage names and branches establish model behavior; their visual readability is a separate acceptance judgment.

The adapter consumes copied frames, derives poses and prop transforms, checks fixed arm reach, binds owner/transfer contact and emits only the model's requested droplet ages. The renderer builds instances from supplied poses and props and rejects duplicate props or missing owners; it no longer chooses a cup from a resident's ID, activity or time. Source contact tests measure actual emitted mesh surfaces as well as virtual contact bindings. The independent probe changes only a supplied frame's time/tick and confirms unchanged pose/prop geometry data after excluding the unused forwarded `pose.time` field. This supports the inspected adapter boundary; it is not a new rendered-pixel or all-clip clearance check.

The main event/history call sites are consistent with the accepted bridge contract. `main.ts:98–108` restores social history, derives the next opening sequence and supplies the direct bridge event sink to mechanism authority. `main.ts:140–143` restores mechanism state and resets bridge attribution. The animation loop observes the current mechanism snapshot before physical advancement, lets emitted completions enter the social journal, then updates social time. Real commands carry `worldTime`, while diagnostic positioning resets the bridge. Camera reset changes camera state only. The history writer preserves unrelated history fields and stores both mechanism state and the social journal/time; pagehide and graphics loss force a save. Persisted page suspension and graphics loss retain the in-memory authority and stop the render loop; resume clears the previous RAF timestamp. These are inspected call-site relationships, not executed browser lifecycle results.

Social-history restoration validates version, nonnegative finite time, dense opening arrays, unique positive safe sequences, known mechanism/source values and finite timestamps before replacing history. Seek replays the retained journal; sorting by life time and then sequence supports new visitor input after backward diagnostic seeking without deleting future history. Equal-time opening insertion queues history without moving the current frame. The independent probe uses four openings, including equal-life-time events on two mechanisms and non-round timestamps, to compare one advance with 1,082 mixed advances to `75.1234567896`, then backward/forward seek and restoration. Complete snapshots match. Five additional malformed histories are rejected without altering the prior snapshot. These finite cases support the stated semantics and do not claim arbitrary-history or unbounded-duration coverage.

No additional material source defect was independently established in this review. This is a bounded result, with F31 unresolved and actual browser/control/performance acceptance still required.

## Findings and disposition

| ID | Finding | Disposition and reason | Repair or follow-up |
|---|---|---|---|
| F31 | Raw versus rounded time comparison in `community.update` repeats full history replay during equal-time paused renders, and can repeat an explicit seek. The original partial reviewer and recovery reviewer reproduce the same 3 resets/909 ticks counterexample. | Unresolved on frozen A2. Reviewer recommendation: repair before phase acceptance. Manager retains disposition and phase-acceptance ownership. | Review the separately supplied exact clock/presentation repair, require a meaningful community-level no-replay control for rounded-up/down times and explicit seek, then run the affected real browser and performance gates. |

## Verification

The independent `output/manager/phase10-source-recovery/review-probe.mjs`, SHA-256 `5801ea357b757de564a8f2277c6e1ac43c511e0699821324238ccceafb6acd70`, exits 0 with Node 24.12.0. Its `review-probe-report.json`, SHA-256 `fa28c8dc5f12d01317e31df6adb40e818fa9d9585058dc6714152aa3437981bc`, binds all 40 input bytes before and after execution, 14 actual modified base blobs, 8 added target hashes, the patch digest, retained original F31 evidence and the exact additional cases above. Source paths were checked for containment and symbolic links. The patch's producer reconstruction record was inspected; this reviewer did not perform another patch application. Probe success means its assertions, including intended F31 reproduction, passed; it does not mean A2 is defect-free.

The following frozen reports under `output/manager/phase10-checkpoint-a2-review/` were inspected against the A2 source hashes and their actual test bodies:

- `producer-social-state.json`, SHA-256 `9fb49ad83291bfc6476f9dd9401aa348b9865d8ecce553fa1b74cd41dbb0556a`: producer pass for 240 seconds at 30 Hz with 26 actors, 51 recorded completions, minimum sampled separation `0.21036123371739188` and no waits in its ordinary sample. A separate actual blocked-approach fixture reaches 557 wait ticks before finite retreat/release. Other cases cover 60-second frame partitions, seek/history, 100 frozen openings, distinct traits and copied inputs/outputs. Reservation, reciprocal-participant, prop-owner and droplet controls corrupt observed snapshots; they validate those assertions and are not production-source mutation proofs.
- `producer-social-approaches.json`, SHA-256 `e236c5405c50ae78014cd44c9a9cbcf8c16916148b58f4c082ddbc9286b8d7fb`: producer pass for five approaches/166 samples plus seven unchanged legacy routes/1,089 samples. The new approach check uses at most 0.025 spacing, nine support rays at 0.085 radius, a 0.42-high body envelope and 0.004 sole-contact allowance. It rejects actual counter/joystick intrusion, off-device/raised support and a lowered real canopy. This samples support and body geometry; it does not inspect all animation parts at every intervening position.
- `producer-social-contact.json`, SHA-256 `43c758e71c1bcdc6ee1f16d54cbe9cd659129a67bbc7fe20a9edf9a400491278`: producer pass for 45 explicit café samples, 24 book samples, 10 watering samples and 7,201 actual model ticks over 240 seconds. Every tick is translated and checked for reach/prop step bounds; actual surface and static-obstacle checks run at selected phase buckets and every transfer, totaling 447 emitted frames, 756 contacts and 8 owner changes. Maximum measured grip gap is `0.00043793785790540064`, lip gap `0.000010962539868368546`, endpoint hand jump 0 and ordinary prop step `0.009220775446018941`. Five controls reject a displaced emitted cup, duplicate prop, unreachable receiver, sideways receiver and emitted head moved into the real roof. The ordinary contact timeline has no opening journal; explicit reaction geometry covers one resident against three opening targets. It does not establish every resident's reaction clip clearance or browser lifecycle behavior.

Adjacent `check-exploration` and `check-mechanism-input` source was inspected for its new social assertions. It requires genuine running/pause state, real Space/R/mechanism keyboard and pointer input, queued opening journals, history return, graphics loss and resource-cycle checks. Synthetic persisted events are labelled separately from actual navigation. Most social preservation assertions compare state and therefore cannot detect F31's repeated identical rebuild. None of these browser commands was executed in this round. Existing native captures and source inspection do not substitute for them or for the normal installed-Chrome timing gate.

The probe disposed its constructed instances, geometries, materials and textures in `finally`, removed its loader hooks, exited without child processes and launched zero browser/GUI/server resources. Its ignored evidence is intentionally retained for unresolved F31 and this review handoff. No earlier evidence was overwritten or cleaned.

## Round outcome

The independent A2 source review is complete within its declared bounds. The model/layout/pose authority and main bridge/history call sites have no further confirmed material defect, but the community integration is not accepted while F31 remains. Preserve A2 and its original replay evidence. Exact-source repair review, actual browser input/lifecycle checks, native visual acceptance of the remaining families and combined performance/integration gates remain necessary before manager phase acceptance.
