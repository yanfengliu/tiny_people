# Review 37: implementation

## Target

Focused source and regression-check review of checkpoint C's F32 gardening repair, against checkpoint B accepted for F31 in [round 36](36_implementation.md). Frozen C is retained at `C:/Users/38909/.codex/worktrees/dead/tiny_people/output/phase10/checkpoint-c/`; the primary repository is `C:/Users/38909/Documents/github/tiny_people`. Its 41-file, 530,496-byte source manifest SHA-256 is `77c1ab97a002131a9753e055293586304ec2e0d2413dfb644d4a22244a19a4b5`. The full `changes-from-2e57ccd.patch` is `c0f76ec223df61f0e43e3ca3b1bcb2f2e358d92b5ead204a1138e3a550ed828f`, against accepted runtime `2e57ccd555a9cc4300abc828916631fb8f2eb097`; the packaging report is `7047a8fedf0610f797d0ad43e5c650ab62aa5e48c412a864930f9b78587fb649`.

Only three B-to-C inputs change. The `delta-from-b/source-manifest.json` SHA-256 is `ed0d971e609755272b4f094ac8f47acb7fbdca547a2fb68299c0dbbc4791e814`; the retained `delta-from-b/changes-from-b.patch` is `c14633d095192f692c5f762a1d9a77b64da4b32968ae01a696532b5d4f03c82f`.

| Reviewed input | SHA-256 |
|---|---|
| `src/scene/social-state.ts` | `d6f07da9e4828987c7997b08519ef612fa11f56c93c3ebee42e129c498ac1a7b` |
| `scripts/check-social-state.mjs` | `b9aacc68a9207fce51da05ad330dd9ab1d78ed27da09aec88d9a088c127b6a31` |
| `scripts/check-social-contact.mjs` | `09957949fb7c6d829405211ef079230e21b6d8c467d9a8577948c0df702b5500` |

The exact B model used by both old-order controls is `9313c85d7e58b1ea022d19b090bb9c5fe4605ca07d9d78bd8adc70b23daa45c1`, also the historical A2 model. This review preserves reports 33–36 and their counterevidence; it does not relabel earlier native frames as C evidence.

## Reviewers and coverage

`/root/phase10_source_recovery` independently reviewed the three-file frozen delta, the F32 concern and accepted correction direction in [round 35](35_implementation.md), the unchanged B dependencies, the producer gate bodies and retained results, and the producer's bounded verification narrative. It verified all C/B/delta bytes, independently reconstructed C using the B delta, and ran a small Node-only comparison of C and the exact old B model on the actual scene layout. This stopped after the first completed cycle of both actual garden pairs and applied the unchanged order assertions extracted from the two frozen C gate scripts.

No browser, Vite instance, localhost server, visible application or GPU workload was launched. The full 240-second state/contact gates and unchanged suites were not repeated. The review makes no new native visual judgment. Only this report and ignored bounded proof files were created; no production source, plan, prior review, AGENTS file, Git index/history or Git configuration changed. The manager owns source acceptance and final disposition; visual review 38 remains the separate F32 readability condition.

## Reports

### `/root/phase10_source_recovery`

The source repair matches the accepted narrow correction. The only changed production line is `social-state.ts:27`: `help` moves from after final acknowledgment to before `water-prepare`. The resulting order is guidance, preparation, watering, drain, lowering and acknowledgment. Each stage's duration remains unchanged, as does the 9.1-second sum of those activity stages. No prop, pose, target, supported approach or circuit position is added or changed.

The unchanged model and pose adapter already supply the needed response at these stages. During guidance, the helper points and looks toward the soil while the gardener attends to the helper. Preparation and tending then direct the gardener's attention toward the soil and lift the held can. Drain retires existing droplets before lowering. Final acknowledgment returns the gardener's attention to the helper. Moving the guidance earlier gives these existing actions the intended temporal relationship. Whether a viewer reads that relationship clearly in an ordinary rendered sequence still requires the independent visual review; stage names and geometry measurements do not decide that question.

The state gate now checks every observed garden trace against an explicit expected sequence independent of the runtime stage array. A partial trace must be an exact prefix, completed traces must contain the entire sequence, and each transition must occur at a later tick. The actual participants are pinned separately to courtyard 17/18 and circuit 20/19. Both opportunities must have a successfully completed interaction, so an empty or too-short observation window cannot pass as cooperation. Per-tick assertions also check the helper's soil-directed cue, the gardener's attention toward the helper during guidance/acknowledgment, and soil attention during preparation, water, drain and lowering. Existing deterministic-time, reservation, ownership, separation, finite-drain and blocked-approach controls remain in place.

The contact gate retains its existing actual timeline and explicit contact checks, then records frames by real interaction ID and stage. It requires the first and last observed times to place guidance before preparation and each tending stage before the next. It checks this order for every completed garden interaction in its ordinary timeline. Separately, it emits and measures selected first/middle/last stage frames from the first completed cycle of each actual garden pair. These two geometry observations are not sixteen complete geometry sweeps.

The new geometry checks reach actual emitted instances and physical soil bounds. The helper's pointing direction uses the emitted right-hand center and the shoulder end of its right sleeve; the renderer emits six limb instances per resident, with that sleeve at index `residentIndex * 6 + 5`. The sleeve's negative local-Y cap is its shoulder endpoint under the existing shoulder-to-sleeve bone transform. The target must match the center of an actual `recessed-soil` mesh. The check then requires a directed arm change, head attention changing from helper to soil and back, intermediate can lift, no drops during guidance/preparation and a dry lowered can before acknowledgment. Existing actual hand/can surface checks still run on those emitted samples. This is a meaningful physical supplement to the ordering check, within the stated sampled bounds.

Both old-order controls execute production model code, rather than rearranging captured traces or comparing a copied stage array. Each gate independently moves the actual first `help` stage to the end; both resulting complete model strings match B byte for byte. The state gate requires both old-model pairs to complete within 120 seconds before rejecting their observed sequences. The contact gate also runs those completed old-model frames through the actual pose/contact checks before requiring the order assertion to fail. The retained result demonstrates why both checks matter: B still has valid pointing/contact geometry, but its guidance arrives after tending.

The independent bounded probe confirms that distinction using the exact C gate order-assertion bodies. On both C and exact old B, the circuit cycle completes at tick 339 and the courtyard cycle at tick 1777. Observation stops at tick 1779, or 59.3 simulated seconds, after the next three-tick completion poll. Both C pairs pass both order assertions. Both old B pairs fail both assertions for the expected guidance-before-tending messages. The probe does not rerun the contact geometry sweep; the geometry results below are inspected producer evidence.

All 38 inputs outside the three-file delta remain byte-identical to B. The F31 community wrapper and time gate, authored layout, poses, resident geometry/materials, mechanism state/bridge and main wiring are unchanged. The only runtime model change is the stage-order line. This preserves the narrow F31 repair mechanism but does not make its prior B run a C run. The separately retained C-bound time report was verified and already passes its nine cases plus the original raw-comparison rejection control, so no duplicate F31 execution was needed. Earlier native reviews remain tied to their original bytes and stated sample limits; the changed garden sequence needs fresh C visual evidence.

No new material source defect was found. The source-side F32 ordering repair and regression controls are acceptable within this scope. F32's final readability disposition remains open for the manager and independent visual reviewer.

## Findings and disposition

| ID | Finding | Disposition and reason | Repair or follow-up |
|---|---|---|---|
| F32 | Earlier gardening showed tending and acknowledgment before the helper's point, leaving its useful contribution ambiguous. | Source-side repair verified on C: guidance precedes tending, existing attention/prop actions remain, both real pairs complete, and both old-order model controls fail meaningful observed-sequence checks. This does not independently close visual readability. | Manager may accept this source scope. Keep final F32 closure pending source-bound visual review 38 and the remaining combined acceptance gates. |

## Verification

Independent `output/manager/phase10-f32-source-review/verify-c.mjs`, SHA-256 `f9f22a2c379710199858dece48499e8e4718223ac4a74b84ee62086f838f627a`, exits 0 with Node 24.12.0. Its `verification.json`, SHA-256 `9d55a1a81d33065445d16bfa48549253346d4693c0daf89aff7b405ad2490c12`, records the frozen manifests, exact changed paths, sole runtime-line change, extracted assertion digests, producer source bindings and the two-pair C/B comparison above. The old model is loaded through a Node hook as exact B bytes; C runs without source instrumentation. This is a bounded independent API execution, not another full Vite gate.

All 41 C files, all 41 B files and the three delta copies match their source manifests before and after execution. Path containment and link absence were checked. Applying the retained B-to-C patch to task-owned exact B copies with `git apply --no-index` and per-command `core.autocrlf=false` independently reconstructs all 41 C hashes. The output directory was checked before use and cleanup. The full actual-base patch and packaging record were hash-verified; a second full actual-base reconstruction was not performed.

The following producer evidence was inspected and its recorded source hashes matched to C:

- Frozen `evidence/f32-state-report.json`, SHA-256 `03bc51e5170be9860d5bde38e05ac1b1480df2b2915622633282eef9fcf8e5db`, passes 240 seconds at 30 Hz with the existing opening inputs and invariant checks. It records 16 completed garden cycles: 13 circuit and 3 courtyard. Every observed trace has the valid prefix and every completed cycle has the full order. The whole run records 51 completed interactions, minimum sampled separation `0.21036123371739188`, and the separate actual blocked-approach control still retreats after at most 557 recorded wait ticks. Both executed old-order garden pairs are rejected.
- Frozen `evidence/f32-contact-report.json`, SHA-256 `2ef731e7256fda8093f6bb6f61f6e2626d2cb8cae6a591499101d36b4446af12`, records the ordinary 240-second timeline without opening inputs: 7,201 model translations, 52 completed interactions, 8 cup ownership changes and 16 completed garden cycles whose order is checked. The detailed F32 geometry observations cover the first complete circuit cycle and first complete courtyard cycle. The whole suite, including fixtures and corruption observations, emits 479 frames and checks 812 contacts. Actual contact/obstacle checks use phase buckets and transfers, not every-tick emitted-mesh clearance. The maximum grip gap `0.00043793785790540064`, lip gap `0.000010962539868368546` and ordinary prop step `0.009220775446018941` remain under unchanged limits.
- The two detailed C garden observations report circuit/courtyard pointing dots `0.9999996787618032` and `0.9999997186835639`, helper hand travel `0.11356251947419452` and `0.10064246516304073`, and intermediate can lifts `0.008656024932861328` and `0.008008003234863281`. Circuit guidance spans `1.6–3.266666667` seconds, water begins at `4.5` and final acknowledgment at `9.7`; courtyard guidance spans `25.766666667–27.433333333`, water begins at `28.666666667` and acknowledgment at `33.866666667`. These measurements establish the inspected geometry/order cases, not viewer interpretation.
- Frozen `evidence/f32-old-order-contact-report.json`, SHA-256 `5faddee18c481176df53374f88e8a3671e3fffbc0b4aab79c81b9274a182c944`, records valid old-order geometry observations and expected order failures for both pairs. The separately retained state/contact old-model files each hash to exact B model `9313c85d7e58b1ea022d19b090bb9c5fe4605ca07d9d78bd8adc70b23daa45c1`.
- The producer's immutable C F31 run at `output/phase10/social-time/runs/2026-09-08T17-00-59-463Z/report.json`, SHA-256 `1b234ba6793749ef7bc3bd403a3f44fada32214daade26e306e3798c03c71f67`, has all 18 before/after inputs matching C, nine passing candidate cases and the expected 3-reset/909-tick raw-comparison rejection. This is inspected C-bound producer evidence, distinct from round 36's independent B acceptance.

Producer `output/phase10/poses/f32/verification.md`, SHA-256 `1dcdd55f2ee05cd6be6c3159e81a02207ccc3cc0441dda8f2707c2cdf644771e`, and `output/phase10/cpu-evidence-inventory-c.md`, SHA-256 `6c2b50a08f0c3a6ddb85423b275ce628e4f4ee37c68161675dc8502d538c8054`, were read with their authorship and limits retained. The broader gate inventory is supporting producer context, not an independent rerun or full acceptance by this reviewer. In particular, fixture preparation and CPU timing-evaluator checks do not establish browser input or measured frame performance.

The probe disposed constructed instances, geometry, materials and textures in `finally`, removed its loader hooks and deleted only its checked reconstruction scratch. Node PID 34800 and the scratch directory were absent at the final check. Zero browser/GUI/server resources were created. Ignored proof files remain for the active review handoff. Reports 33–36 were rehashed before and after and remain unchanged; previous failed source, raw proofs and native captures were not modified.

## Round outcome

Accept the exact C gardening-order source repair and the meaningful two-gate old-model rejection controls within the stated bounds. No additional source repair or duplicate CPU suite is requested. Final F32 visual readability, full real input/lifecycle/performance checks and combined integration acceptance remain manager-owned and pending; phase 10 is not declared complete.
